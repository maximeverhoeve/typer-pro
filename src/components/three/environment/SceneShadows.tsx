import React from 'react';

const SceneShadows: React.FC = () => {
  return (
    <>
      {/* <ContactShadows
        rotation={[-Math.PI / 2, 0, 0]}
        position={[0, -1.5, 0]}
        far={2}
        color={color}
        opacity={opacity}
        blur={blur}
      /> */}
    </>
  );
};

export default SceneShadows;
